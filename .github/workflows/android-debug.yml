name: Faithbook Android Debug APK
on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - name: Install Android SDK 35
        run: sdkmanager "platform-tools" "platforms;android-35" "build-tools;35.0.0"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'
      - name: Static Faithbook verification
        run: python3 tools/verify_public_v19.py
      - name: Build debug APK
        run: gradle --no-daemon :app:assembleDebug --stacktrace
      - name: Hash APK
        run: sha256sum app/build/outputs/apk/debug/app-debug.apk | tee Faithbook-debug.apk.sha256
      - uses: actions/upload-artifact@v4
        with:
          name: Faithbook-debug-apk
          path: |
            app/build/outputs/apk/debug/app-debug.apk
            Faithbook-debug.apk.sha256
          if-no-files-found: error
